import { FC } from 'react';
import { Button } from '@mui/material';

const Sup: FC = () => {

  return <>
    <button>Works?</button>
    <Button color='primary'>Work</Button>
  </>;
};

export default Sup;
